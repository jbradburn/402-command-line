#include <stdlib.h>
#include <stdio.h>

#include "filelib.h"
#include "globals.h"

int main(int argc, char **argv)
{
    if (argc != 2) {
        return -1;
    }
    if (argv == NULL) {
        return -1;
    }

    return 0;
}
